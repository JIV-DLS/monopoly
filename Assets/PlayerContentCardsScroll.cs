using UnityEngine;
using TMPro;

public class PlayerContentCardsScroll : MonoBehaviour
{
    public GameObject CardItemPrefab; // Prefab for the tab button

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
